<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/ mybatis-3-mapper.dtd">

<mapper namespace="Board">
    
	<insert id="fileInsert" parameterType="com.walab.oas.Board.domain.FileVO">
    	INSERT INTO files (fno, bno, filename, fileOriName, fileurl)
    	VALUES (#{fno},#{bno},#{fileName},#{fileOriName},#{fileUrl})
    	<!-- SELECT files.*, BOARD.seq FROM files LEFT OUTER JOIN BOARD ON files.bno = BOARD.seq -->
	</insert>

	<insert id="insertBoard" parameterType="com.walab.oas.Board.BoardVO" useGeneratedKeys="true" keyProperty="seq">
		insert into BOARD (category, title, writer, content, photo)
		values
		(#{category} , #{title} , #{writer} , #{content}, #{photo})
	</insert>
	<update id="updateBoard">
		update BOARD
		set title=#{title}, content=#{content},
		writer=#{writer},
		category=#{category},
		photo=#{photo} where seq=#{seq}
	</update>
	<delete id="deleteBoard">
		delete from BOARD where seq=#{seq}
	</delete>
	<select id="getBoard" resultType="board">
		select * from BOARD where
		seq=#{seq}
	</select>
	<select id="getBoardList" resultType="board">
		select * from BOARD order
		by seq desc
	</select>
</mapper>